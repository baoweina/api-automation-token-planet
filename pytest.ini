[pytest]
pythonpath = .
testpaths = testcases
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --alluredir=reports/allure-results --clean-alluredir
markers =
    smoke: 核心冒烟用例
    negative: 反向/异常场景用例
    needs_auth: 依赖登录态（AUTH_TOKEN）的用例
